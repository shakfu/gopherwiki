# Build artifacts
build/
bin/

# Binaries
# Anchored to the repo root so it only ignores the built binary, not the
# cmd/gopherwiki/ source directory (which contains embedded files like
# syntax_guide.md that must be tracked for `go:embed` to build in CI).
/gopherwiki
*.exe
*.exe~
*.dll
*.so
*.dylib

REVIEW.md

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# App data
app-data/
repository/
*.sqlite
*.db

# Environment
.env
.envrc

# Docker
docker-compose.override.yml

# Editor build
web/editor/node_modules/

# Logs
*.log

# agents
.claude
CLAUDE.md
AGENTs.md
GEMINI.md

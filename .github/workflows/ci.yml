name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Vet
        run: go vet -tags fts5 ./...

      - name: Test
        run: go test -tags fts5 -v -race -count=1 ./...

      - name: Build
        run: go build -tags fts5 -ldflags="-s -w" -o /dev/null ./cmd/gopherwiki

  editor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install editor dependencies
        run: bun install --frozen-lockfile
        working-directory: web/editor

      - name: Build editor bundle
        run: make build-editor

      - name: Verify committed bundle is up to date
        run: |
          if ! git diff --exit-code -- web/static/js/editor.bundle.js; then
            echo "::error::web/static/js/editor.bundle.js is stale. Run 'make build-editor' and commit the result."
            exit 1
          fi

  docker:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

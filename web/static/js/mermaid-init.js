// Initialize Mermaid diagram rendering. Kept in an external file (rather than an
// inline <script>) so the page can be served under a Content-Security-Policy
// without requiring 'unsafe-inline' for this block.
mermaid.initialize({ startOnLoad: true });
